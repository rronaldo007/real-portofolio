# Technical Defaults

> **This file is a stub.** Fill it in as the project's conventions solidify. Keep it short — capture the conventions that aren't obvious from reading the code.

## Stack

<!-- Languages, frameworks, runtime versions, DB, cache, queue. One line each. -->

## Structure

<!-- Top-level directories and what lives in each. Mirror what's in `CLAUDE.md` → Project Structure, but with more detail. -->

## Coding Standards

<!-- Non-obvious conventions only. Examples:
- Primary keys: UUIDs everywhere (no sequential IDs)
- All models extend `BaseModel` (created_at, updated_at)
- Settings split into base/dev/prod
-->

## Patterns to Follow

<!-- 3–5 load-bearing patterns. Examples:
- Feature gating via `RequireFeature("x")` permission
- Server state via React Query; client state via Zustand
- Background work via Celery tasks in `tasks.py` per app
-->

## Patterns to Avoid

<!-- Anti-patterns that have hurt the project before. Keep each one tied to a concrete reason. -->
